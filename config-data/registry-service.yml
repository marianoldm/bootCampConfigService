server:
  port: 8099

eureka:
  instance:
    hostname: localhost
  client:
    #no se registre eureka service dentro del mismo eureka
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8099/eurekaaaa/